# Heavy dependencies (pandas, aws-lambda-powertools, pg8000) should be
# added as Lambda Layers on AWS - not bundled in the deployment package.
# Only add lightweight packages here that aren't in your layers.
